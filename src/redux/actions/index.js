import testAction from "./testAction";
import authAction from "./authAction";
import surveyAction from "./surveyAction";
import detiAction from "./detiAction";

const Actions = {
    test: testAction,
    auth: authAction,
    survey: surveyAction,
    deti: detiAction,
}

export default Actions;